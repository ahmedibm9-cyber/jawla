<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DemoSeeder extends Seeder
{
    public function run(): void
    {
        // One company, 3 routes, ~15 customers, ~20 products, 3 reps
        // with loaded vans. Print each seeded user's credentials to
        // stdout so the developer can log in. Implemented in Phase 12.
    }
}
