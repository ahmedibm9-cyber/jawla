# Story B7.3 -- PO Generation Admin Action

**Status:** ready-for-dev
**Epic:** B7 -- Purchase Requests
**Investigation reference:** bmad-output/investigation-b7-purchase-requests-completion-2026-07-19.md
**Estimated effort:** Small (~0.5 days)
**Blocked by:** B7.2
**Labels:** admin, purchase-order, p0

---

## Story

**As a** Purchasing Manager
**I want** A Generate PO button on purchasing_approved requests
**So that** I can manually trigger PO creation if auto-generation fails or for re-generation

---

## Acceptance Criteria

### AC 1: Action visible only when status = purchasing_approved && user has purchasing role
- Action visible only when status = purchasing_approved && user has purchasing role

### AC 2: Calls PurchaseOrderService::createFromPurchaseRequest()
- Calls PurchaseOrderService::createFromPurchaseRequest()

### AC 3: Shows success toast with PO number link
- Shows success toast with PO number link

### AC 4: Idempotent -- if PO already exists, show existing PO link instead of creating duplicate
- Idempotent -- if PO already exists, show existing PO link instead of creating duplicate

---

## Technical Details

### File: app/Filament/Resources/PurchaseRequestResource.php

Add action after purchasing_approve:

Add action after purchasing_approve:

Action::make(\
generate_po\)
    ->label(__(\
app.generate_po\))
    ->icon(\
heroicon-o-document-plus\)
    ->color(\
success\)
    ->visible(fn (PurchaseRequest \) => \->status === \
purchasing_approved\ && auth()->user()->hasAnyRole([\admin\, \purchasing\]))
    ->action(function (PurchaseRequest \) {
        try {
            \ = app(PurchaseOrderService::class)->createFromPurchaseRequest(\);
            Notification::make()
                ->title(__(\
app.po_generated\, [\number\ => \->po_number]))
                ->success()
                ->send();
        } catch (\Exception \) {
            if (str_contains(\->getMessage(), \
duplicate\)) {
                Notification::make()->title(__(\
app.po_already_exists\))->warning()->send();
            } else {
                throw \;
            }
        }
    })
    ->requiresConfirmation(),

---

## Verification Steps

1. Navigate to purchasing_approved purchase request
2. Click Generate PO -> verify PO created, toast shows PO number
3. Click again -> verify shows existing PO instead of error

---

## Dev Notes

- Investigation reference: bmad-output/investigation-b7-purchase-requests-completion-2026-07-19.md
- Risk: Low -- thin wrapper around existing service
- Dependencies: B7.2 (PurchaseOrderService must exist)

---

## Definition of Done

- [ ] Generate PO action added to PurchaseRequestResource
- [ ] Action visible only for purchasing_approved + purchasing role
- [ ] Calls PurchaseOrderService::createFromPurchaseRequest()
- [ ] Success toast with PO link
- [ ] Idempotent: shows existing PO if already generated
- [ ] All tests pass
